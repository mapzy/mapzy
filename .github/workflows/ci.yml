name: CI

on: [push]

jobs:
  ci:
    runs-on: ubuntu-latest

    env:
      DATABASE_HOST: localhost
      DATABASE_USERNAME: root
      DATABASE_PASSWORD: root
      DATABASE_NAME: mapzy_test
      RAILS_ENV: test
      NODE_ENV: test

    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_USER: root
          POSTGRES_PASSWORD: root
        ports:
          - 5432:5432
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.0.0
          bundler-cache: true

      - name: Set up Node
        uses: actions/setup-node@v2
        with:
          node-version: '14'
          cache: 'yarn'
      - run: yarn install

      - name: Setup test database
        run: |
          bin/rails db:create
          bin/rails db:schema:load

      - name: Run security checks
        run: |
          bin/bundler-audit --update
          bin/brakeman -q -w2

      - name: Run linters
        run: |
          bin/rubocop --parallel

      - name: Run specs
        run: |
          bundle exec rspec spec --color --format progress